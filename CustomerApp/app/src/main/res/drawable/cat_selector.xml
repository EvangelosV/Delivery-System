<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Selected state -->
    <item android:state_selected="true" android:drawable="@drawable/cat_selected" />

    <!-- Pressed state -->
    <item android:state_pressed="true" android:drawable="@drawable/cat_selected" />

    <!-- Default / unselected state -->
    <item android:drawable="@drawable/cat_unselected" />

</selector>
